using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrbitRotation : MonoBehaviour
{    
    void Start() {
     
    }

    void Update() {
        // GameObject parentPlanet = transform.parent.gameObject;
        // transform.RotateAround(parentPlanet.transform.position, parentPlanet.transform.up, 50 * Time.deltaTime);
    }
}